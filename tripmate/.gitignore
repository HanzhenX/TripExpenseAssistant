# Node modules (both in root and parent dir — double-check why ../node_modules exists)
node_modules/
../node_modules/

# Environment variables
.env
.env.local

# Build output
.next/

# OS or IDE stuff (just in case)
.DS_Store

# Optional: lock files in parent dir if you only want to track the ones in your project folder
../package-lock.json
../package.json

package-lock.json

prisma/migrations/

src/app/upload-test/

# npm install @aws-sdk/client-s3
# npm install @aws-sdk/s3-request-presigner